export enum GENDER {
    FEMALE = 'female',
    MALE = 'male',
    OTHER = 'other'
}

export enum RelatedType {
    POST = 'post',
    COMMENT = 'comment'
}

export enum POST {
    POST = 'post'
}

export enum COMMENT {
    COMMENT = 'comment'
}